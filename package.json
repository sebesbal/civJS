{
  "name": "civjs",
  "version": "1.0.0",
  "private": true,
  "description": "CivJS smoke tests",
  "scripts": {
    "test:smoke": "playwright test",
    "test:smoke:headed": "playwright test --headed",
    "test:smoke:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
